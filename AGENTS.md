# proxima-hub — Agent instructions

Front door / launcher de la suite PROXIMA en app.proxima.pe (identidad SSO + app-switcher +
cuenta/billing). App Angular delgada. "Qué NO es: un ERP" — solo la puerta de entrada.

## Lectura inicial
1. `ARCHITECTURE.md` — qué es el Hub, el mapa de la suite, la delegación de auth SSO.
2. `README.md`.

## Comandos uniformes (Engineering System)
`mise run dev|lint|test|build|verify|ci|doctor` — fachadas de pnpm (`verify`=build; sin lint aún).

## Desarrollo de features — harness OpenSpec↔Beads (ecosistema PROXIMA)
OpenSpec define el plan, Beads administra la ejecución. Verbos:
`mise run beads-sync-dry|beads-sync|beads-next|beads-verify|beads-progress|beads-review-gate <change>`.
Skills: `/change-plan` → `/change-run` → `/change-run-auto` → `/change-review`.
`tasks.md` en formato `Txxx` + Verification + Acceptance (`../proxima-engineering/harness/docs/task-format.md`).

## Reglas duras
- Es la única identidad de la suite: admin/builder/pos delegan su login acá (SSO). No romper ese contrato.
- App delgada: NO meter lógica de ERP/negocio; eso vive en proxima-admin.

<!-- BEGIN BEADS CODEX SETUP: generated by bd setup codex -->
## Beads Issue Tracker

Use Beads (`bd`) for durable task tracking in repositories that include it. Use the `beads` skill at `.agents/skills/beads/SKILL.md` (project install) or `~/.agents/skills/beads/SKILL.md` (global install) for Beads workflow guidance, then use the `bd` CLI for issue operations.

### Quick Reference

```bash
bd ready                # Find available work
bd show <id>            # View issue details
bd update <id> --claim  # Claim work
bd close <id>           # Complete work
bd prime                # Refresh Beads context
```

### Rules

- Use `bd` for all task tracking; do not create markdown TODO lists.
- Run `bd prime` when Beads context is missing or stale. Codex 0.129.0+ can load Beads context automatically through native hooks; use `/hooks` to inspect or toggle them.
- Keep persistent project memory in Beads via `bd remember`; do not create ad hoc memory files.

**Architecture in one line:** issues live in a local Dolt DB; sync uses `refs/dolt/data` on your git remote; `.beads/issues.jsonl` is a passive export. See https://github.com/gastownhall/beads/blob/main/docs/SYNC_CONCEPTS.md for details and anti-patterns.
<!-- END BEADS CODEX SETUP -->
